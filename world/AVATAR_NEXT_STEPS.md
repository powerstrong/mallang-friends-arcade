# 아바타 꾸미기 — 개선 백로그 (2026-08-17 사용자 1차 리뷰 반영)

> 설계 근거는 [AVATAR_DESIGN.md](./AVATAR_DESIGN.md). 이 파일이 다음 세션의 작업 목록이다.
> 상태: A단계 + **카탈로그 v3(성별 전용 27종)** + P0 두 건 + P2-1 + P2-3 + **걸음 감성 v2 완료(2026-08-18)** — 워커 64 pass(패리티 포함), 뱃지 v44.

## ~~2차 리뷰 대응~~ 완료(2026-08-18) — 남녀 겹침 제거 + 옷장 2배 확장

**지시**: ① "남자인데 긴머리인가 치마가 있었다. 말이 되냐" ② "좀 더 다양한 아바타 옵션을 만들어라. 남자용이랑 여자용은 겹치지 않게 제작해라" ③ "걷기 감성은 더 높이려면 어떤 게 가능하냐"

**원인(①)**: 헤어 2종이 `fit:'all'` 이었다 — P0-2에서 옷만 태깅하고 헤어를 빠뜨렸다. 치마(`fit:'girl'`)는 UI 필터가 이미 막고 있었으므로, 남아에게 보인 것은 **긴 생머리**다. 설계서 §3 이 `all` 을 허용한 것 자체가 화근이라 정책을 폐지했다(§3 재개정).

**실행 결과**:
1. **`fit:'all'` 폐지** — 옷·헤어·모자 전부 girl|boy 전용. 공용으로 남긴 것은 동글 안경 하나(사유는 §3). 기존 공용품 재배치: 하늘 세트·청바지 → boy / 딸기 티·라벤더 긴팔 → girl / 긴 생머리 → girl / 짧은 머리 → boy / 베레모 → girl.
2. **신규 12종 반입**(옷장 15 → 27종, 슬롯별 성별당 3종):
   - 헤어 4: 단발머리·양갈래(girl) / 곱슬머리·스포츠머리(boy)
   - 하의 3: 하늘 주름치마·라즈베리 바지(girl) / 조거 팬츠(boy)
   - 상의 2: 프릴 블라우스(girl) / 줄무늬 티셔츠(boy)
   - 한벌옷 2: 별밤 원피스(girl) / 우주복(boy)
   - 모자 1: 야구모자(boy)
   - 생성 성적 = 1차 12건 중 10건 통과, 재생성 3건(보브 = 머리중심 2.5px FAIL → 좌우대칭 문구 추가로 0.5px, 양갈래 = 정면에서 갈래가 안 보임, 곱슬 = 보브와 구분 안 됨 → 둘 다 "정면에서 보여야 한다/귀가 드러나야 한다" 실패조건 명시로 해결).
3. **걸음 감성 v2**(world.js) — 아래 별도 절.
4. 카탈로그 v3 양쪽 미러 + 패리티 테스트 통과, `build_assets.sh` 를 전 아이템 커버로 재작성(그동안 P0-2 신규분이 스크립트에 빠져 있었다).

**검증**: 워커 64 pass / 브라우저에서 에셋 58 URL 전부 200 · 성별 겹침 = 안경 1건(의도) · 랜덤 코디 800회에 성별 누출 0 · XOR 위반 0. **실기기 감성 판정은 P1**.

## 걸음 감성 v2 (2026-08-18) — 3열 시트로 낼 수 있는 최대치

**전제**: 프레임을 늘리는 정공법(4~6열 워크사이클)은 **막혀 있다**. 마네킹을 다시 만들어야 하는데 `align.json` 의 row_dy=[0,4,10]이 마네킹 세대 고유값이라 재생성 금지 — 27종 전 에셋을 다시 뽑아야 한다. 그래서 코드 레버만 썼다.

| 레버 | 적용 | 왜 |
|---|---|---|
| 비대칭 타이밍 | 접지 165ms / 통과 85ms (사이클 500ms) | 균등 4박자(130×4)는 발이 뭉개져 로봇처럼 보인다. 통과 자세는 스쳐야 한 걸음이 또렷하다 |
| 상하 바운스 | 접지 중앙 +2.2px, 통과 중앙 −2.2px (코사인) | 없으면 미끄러지듯 이동한다. 두 극점 간격이 정확히 사이클/4라 코사인 한 줄로 맞는다 |
| 위상 개인화 | 걷기 시작 시각 기준 재생, id별 상태 Map | 전역 시계를 쓰면 광장의 모두가 같은 발을 동시에 내딛어 군무가 된다 |
| 정지 여운 | 멈춘 뒤 140ms 동안 바운스 감쇠 | 뚝 끊으면 몸이 튄다 |
| 접지 그림자 | 사람만, 바운스와 연동해 크기·농도 변화 | 동물 시트는 셀 패딩이 제각각이라 여전히 금지(기존 주석 유지). 사람 시트는 384² 정규화라 발 위치가 모든 착장에서 같다 |

되돌리기: `HUMAN_STEP_MS`/`HUMAN_PASS_MS` 를 같은 값으로 두면 구 4박자, `HUMAN_SHADOW=false` 로 그림자만 끌 수 있다. 이름표는 바운스에서 제외했다(따라 떨면 어지럽다).

**아직 안 한 것**(효과는 있으나 실기기 판정 후가 맞다): 이동 속도 연동 프레임레이트(현재 MOVE_SPEED 고정이라 의미 적음) · 방향 전환 스쿼시 · 출발 시 앞으로 기울기 · 걷기 셀 미세 회전으로 중간 프레임 흉내.

## 다음 세션 시작 지점 (2026-08-18 2차 세션 종료 시점의 남은 일, 우선순위순)

1. **P1 사람검증 게이트** — 실기기·2클라, 아래 체크리스트. 코드 작업 아님(사용자와 함께). **걸음 v2 감성 판정과 신규 12종 확인이 최우선.**
2. **P2-2 편집 중 매칭 모달 충돌** — 패널 열림 동안 부스 zone 진입 안내 보류(클라 zone 안내만, 서버 아님) 또는 매칭 제안 시 패널 위 배너.
3. **P2 나머지 소형**: 신발 분리(v3 슬롯) · 헤어색 확장(빌드만) · 피커 카드 걷기 미리보기 · 랜덤 코디 연출.
4. **P3 부채**: §11 CI 패리티는 해결됨(테스트 반입). 남은 것 = 속옷 비침 근본해결(bare-base 시트) · **썸네일 캐시 분리** — 옷장이 27종이 됐다. 성별 필터 덕에 한 탭에 뜨는 건 슬롯당 3종이라 아직 LRU 24 안이지만, 헤어 탭은 3종×4색=12 썸네일이라 아바타 본체 시트를 밀어낼 여지가 생겼다(밀리면 재합성 깜빡임). 다음 확장 전에 아바타/썸네일 캐시를 나눌 것 · sw.js PRECACHE 아바타 미포함(저우선).
5. **B단계**(설계서 §8~10): D1 프로필·옷장·선물함·부모 PIN·쿠폰. fit 태그는 스키마에 선반영할 것.
6. (관찰) 서버 `sanitizeOutfit(d?.outfit)` 이 presetHint 없이 호출됨 — 무효 착장 치환 시 남아도 girl 기본(원피스)으로 치환됨. 실사고는 카탈로그 동기가 지키지만, B단계에서 outfit_change/join 에 preset 실어 해소 검토.

## ~~P0-1~~ 완료(2026-08-18). 모자·안경 부착감 — 결과 기록

**증상**: 모자가 캐릭터 머리와 다르게 움직여 쓰고 있는 느낌이 안 남.

**실측 원인 2가지** (정본/레이어 시트의 셀별 콘텐츠 top(y) 측정, 384² 기준):

| 레이어 | 실측 | 문제 |
|---|---|---|
| 정본 바디 머리 top | row0 [10,10,10] / row1 [12,12,12] / row2 [15,15,15] | 행 내 균일(기준) — 머리 까딱임은 셀 그림 안의 기울기/1px 이동뿐 |
| 베레모 top | row0 [8,8,8] / row1 [9,9,9] / row2 [3,3,3] | **방향별 머리와의 간격이 앞+2 / 옆+3 / 뒤+12px** — 방향 돌 때 모자가 점프, 뒷모습에서 붕 뜸 |
| 안경 top | row0 [43,43,43] / row1 [43,38,38] | **옆모습 걷기 셀에서 5px 위로 점프** — 4박자 재생(A→정지→B→정지)마다 안경이 들썩 |

**원인 해석**: ① 착용 프롬프트(p5v2)의 "걷기 셀에서도 모자 위치·크기·각도는 정지 셀과 동일" 지시가 **캔버스 기준 고정**으로 해석돼 머리의 미세 움직임을 모자가 안 따라감 + ② 방향(행) 간 상대 높이 검수 기준이 없었음.

**실행 결과(2026-08-18, 커밋 552088f)** — 계획 1·3은 그대로, 2는 안경에서 발동:
1. `--fit-head REF --fit-anchor global|row` 구현(모자=global 갭 통일, 안경=row 걷기 점프만 제거). 잔차 게이트 그룹 내 ≤1px + **액세서리 밴드(48px)만 보존**(이동 고스트 스펙 제거). 베레모 갭 앞-2/옆-3/뒤-5 → 전부 -2 통일 PASS.
2. **측정 함정 실측**: cell_metrics 의 최대 블록이 추출 고스트(몸 재음영, 셀당 200~400px)를 잡아 첫 시도가 에셋을 깨뜨림 → `block='first'`(최상단 유의미 블록) 측정으로 해소. 추출도 `--extract-fine`(침식 없는 측지 이력 연결) 추가 — 모자 고스트 자연 배제.
3. **안경은 재생성이 정답이었다**: 철사테(2px)가 추출 침식에 소멸(셀당 알파 25px 파편) → 네이비 뿔테로 재생성(r0/r1 top 37/38 균일, 걷기 점프 0). fine 모드는 눈 재음영을 물어와 안경은 **기본 추출** 유지.
4. 프롬프트 '정지 셀과 동일' → '두상 기준 상대 고정' 교체 완료(p5v2·p6v2). 합성 9셀 육안 검수 완료 — 실기기 재확인은 P1.

## ~~P0-2~~ 완료(2026-08-18). 남녀 옷 구분 — 결과 기록

**지시**: "쿠키·젤리는 남자·여자인데 옷이 같은 게 말이 되냐. 치마류는 여자 것, 남자 옷은 따로."
→ 설계서 §3 유니섹스 정책 폐기(개정 기록 완료).

**실행 결과(2026-08-18, 커밋 a13c709 + 에셋 커밋)** — 계획 그대로 전부:
1. `fit: girl|boy|all` 태그 양쪽 미러 + `itemsFor`/`outfitFitsPreset` 헬퍼. 그리드·랜덤·defaultSeparate(fit===프리셋 우선으로 일반화)·피커 복원 치환 적용. sanitize 는 관대 유지.
2. 태깅: 원피스 2종·레몬 치마=girl / 하늘 세트·딸기 티·라벤더 긴팔·청바지·헤어·모자·안경=all / 남아 라인=boy. **확정은 아빠 검수 때**.
3. 신규 5종 반입(1차 생성 5/5 성공, 재생성 0회): 남아 = 공룡 후드 세트(outfit_hoodie_dino)·체크 남방(top_shirt_check)·로켓 맨투맨(top_sweat_navy)·카고 반바지(bottom_cargo_sand), 여아 = 도트 원피스(outfit_dress_dot). 전부 free.
4. **카탈로그 미러 패리티 테스트**(worker/tests/wardrobe-catalog-parity.test.mjs, P3-2 조기 도입) — id/slot/fit·프리셋·팔레트·catalogVersion 파싱 비교.

## P1. 사람 검증 게이트 (실기기, 사용자)

- [ ] **걸음 v2 감성** — 접지/통과 비율(165/85), 바운스 2.2px, 접지 그림자 농도. 상수는 world.js 상단 한 곳
- [ ] **여럿이 동시에 걸을 때** 발이 제각각인지(위상 개인화가 목적) — 2클라 필수
- [ ] **신규 12종** 착용감 — 특히 스포츠머리와 짧은 머리가 구분되는지, 양갈래 옆모습, 야구모자 뒷모습
- [ ] 각 성별 슬롯 3종씩 보이는지(젤리에 남아 라인·쿠키에 치마/긴머리가 안 뜨는지)
- [ ] 2클라 착장 실시간 반영·저장 반짝임
- [ ] 꾸미기 패널 조작감(아이 손), 6탭 배치, 미리보기 안무(젤리 한바퀴+콩콩 / 쿠키 두리번+폴짝)
- [ ] 거울 오브젝트(600,690) 위치·시인성
- [ ] 사람 아바타로 부스 매칭 → 게임 정상 시작(게임 캐릭터는 각 게임 자체 선택)
- [ ] 옆모습 긴머리 등 어색한 셀 → 해당 시트만 재생성
- [ ] 모자·안경 4방향 걷기 부착감(P0-1 재빌드분) + 신규 5종 옷 실장 확인
- [ ] 쿠키(남아) 프리셋에서 치마·원피스 비노출, 젤리(여아)에서 남아 라인 비노출 확인

## P2. UX 개선 (다음 세션 후보, 작은 것부터)

1. ~~**✕ 닫기 시 편집 초안 유실**~~ 완료(2026-08-18): `wardrobeStash {preset, draft}` 초안 자동 보관 — `closeWardrobePanel()` 에서 `outfitKey(draft)≠outfitKey(saved)` 면 스태시, `openWardrobePanel()` 에서 스태시.preset===현재 프리셋이면 sanitize 복원(아니면 폐기), 저장 버튼은 `wardrobeSaved=draft`+스태시 클리어 후 닫아 이 분기를 안 탐. 재오픈 시 미저장 편집 복원, 저장/프리셋 변경 시엔 폐기.
2. **편집 중 매칭 모달 충돌** — 부스 위에서 꾸미기 열면 매칭 제안이 패널 뒤에 깔림. 편집 중엔 부스 후보 진입을 보류하거나(서버 아님, 클라 zone 안내만), 매칭 오면 패널 위로 안내 배너.
3. ~~**저장 스로틀 드랍**~~ 완료(2026-08-18): 클라 `sendOutfitChange` 가 1050ms 창을 계산해 지연 재전송 — 지연 발사는 발사 시점의 최신 `me.outfit`/`myOutfitRev` 를 실어 연속 저장을 자연 병합. 유실 0.
4. **신발 분리** — 현재 신발은 정본 바디에 베이크(분리옷이면 항상 같은 신발). v3 슬롯 후보.
5. **헤어 색 확장** — 팔레트 4색에 파랑·보라 등 추가(hair_palette.py 3단 치환이라 재생성 없이 빌드만).
6. **피커 카드 걷기 미리보기** — 사람 카드 선택 시 정면 걷기 재생(현재 정지 셀). 소소한 생동감.
7. **랜덤 코디 연출** — 🎲 누를 때 미리보기 반짝/회전 한 바퀴 등 작은 재미.

## P3. 기술 부채·인프라

1. **정적 배포 랙 20~40분(2026-08-17 관측)** — Cloudflare 쪽 git 연동 빌드가 병목. 대시보드(Workers & Pages → 프로젝트 → Deployments)에서 빌드 큐/실패 확인 필요. 저장소에서는 통제 불가 — **뱃지 버전(vNN)과 sw.js CACHE 동기 관례**로 반영 여부 판별(현재 v41).
2. **카탈로그 클라/서버 미러 수동 동기화** — 아이템 추가가 잦아지면 사고 확률↑(한쪽만 고치면 서버가 프리셋으로 치환 — "저장했는데 남에게 기본 코디" 증상). §11 CI 해시 비교를 B단계 전에 조기 도입 권장(worker/tests에 두 파일 파싱 비교 테스트 1개면 됨).
3. **속옷 비침의 근본 해결** — 분리옷 실루엣 규칙("탱크톱·반바지를 완전히 덮기")은 프롬프트 규율에 의존. 크롭티·짧은 치마를 열려면 **bare-base 시트**(속옷을 피부로 편집한 바닥 전용 시트) 1장 생산이 정공법(마네킹 비노출 원칙과 무관 — 바닥 레이어 전용).
4. **썸네일 캐시** — 아이템이 20+가 되면 LRU 24를 아바타/썸네일로 분리 검토.
5. **오프라인 첫 방문** — sw.js PRECACHE에 아바타 에셋 미포함(저우선, 재방문은 런타임 캐시로 커버).

## B단계 연결 (설계서 §8~10)

D1 프로필·옷장·선물함(0005 마이그레이션), 부모 PIN(서버 잠금), 쿠폰+종이 코드(원자성), 코디 저장 3칸, 주간 무료 옷(영구 추가형). **P0-2의 `fit` 태그를 B단계 스키마에 선반영**(소급 변경 방지). 특별 코디 추가 시 무료율 70% 규칙(§8).

## 새 아이템 추가 레시피 (확정 파이프라인)

```bash
# ① 프롬프트: scripts/avatar/p*v2_*.md 중 유형 맞는 것 복사 — "착용 편집" 형식 유지
#    (한벌옷=p1·p2 / 상의=p7v2·p8v2 / 하의=p9v2·p10v2 / 헤어=p3v2 / 모자=p5v2 / 안경=p6v2)
cd scripts/avatar
BODY="C:/src/incubating/web-game-lab/scripts/avatar/raw/mannequin_v1_raw.png"
sed "s|__BODY__|$BODY|" pNEW_item.md | codex exec -s workspace-write --skip-git-repo-check - > pNEW_item.log

# ② 가공 (repo 루트에서):
#    한벌옷:   process.py RAW OUT.png --align scripts/avatar/align.json --ref world/assets/avatar/_mannequin.png
#    상의/하의: process.py RAW --metrics-only ... (착용 시트 QA PASS 확인) 후
#              process.py RAW OUT.png --extract-worn raw/mannequin_v1_raw.png --align align.json
#              → 추출 후 alpha px 수 확인(0이면 생성이 지시를 무시한 것 — 재생성)
#    헤어:     --extract-worn + --split-hair → hair_palette.py 4색
#    모자:     --extract-worn --extract-fine --fit-head OUT/_mannequin.png --fit-anchor global
#    안경:     --extract-worn --fit-head OUT/_mannequin.png --fit-anchor row (fine 금지 — 눈 재음영 유입)
# ③ 카탈로그 등록: shared/wardrobe_catalog.js + worker/src/wardrobe.js **양쪽**(fit 태그 포함)
# ④ wardrobe-preview.html 육안+수치 검수(분리옷은 상·하의 셀렉트) 후 커밋
```

## 함정 목록 (실측 누적)

- **부유 레이어 생성 금지** — 반드시 착용 편집("Image 1을 편집, 해당 아이템만 변경, 불변 조건 나열").
- **상·하의 프롬프트 2대 규칙**: ① "모든 9셀에서 X를 Y로 교체. 어느 셀에도 X가 보이면 실패" 강조(치마 1차가 지시 통째 무시, 차분 0px 전례) ② 실루엣은 탱크톱/반바지를 완전히 덮는 크기만. 추출 후 alpha px 즉시 확인.
- **액세서리 프롬프트에 "정지 셀과 동일" 금지** — 캔버스 고정으로 해석돼 머리를 안 따라감(P0-1). "두상 기준 상대 고정"으로 쓸 것.
- **헤어 프롬프트에는 실패조건을 형태로 적어야 한다**(2026-08-18 실측 3건): "양갈래" 만으로는 갈래가 뒤로만 가서 정면이 단발처럼 나오고, "짧은 곱슬"만으로는 보브와 구분이 안 됐다. `정면 셀에서 갈래가 얼굴 좌우로 보여야 한다` / `귀가 완전히 드러나고 목덜미가 보인다 — 단발처럼 보이면 실패` 처럼 **보여야 할 것과 실패 판정**을 함께 쓰면 1회에 통과한다.
- **헤어 착용시트 QA 의 머리중심 편차는 좌우 비대칭에서 터진다** — 보브 1차가 2.5px(게이트 2). `9셀 전부에서 머리 실루엣이 두상 중심선 기준 대칭` 한 줄로 0.5px 로 떨어졌다. 한쪽만 귀 뒤로 넘긴 머리가 주범.
- **모자류는 착용시트 QA "정본 대비 머리 중심"이 항상 FAIL** — 모자가 머리 실루엣을 키우기 때문(베레모 2.5 / 야구모자 5.5px). 이 지표로 모자를 판정하지 말 것. 실게이트는 `--fit-head` 잔차(그룹 내 ≤1px).
- **헤어 parts.back 등록 기준 = master_back 알파 ≥500px**(실측: long 2197 / bob 751 / twin 611 = true, short 45 / curly 275 / crop 7 = false). 그 아래는 턱선 분할이 흘린 부스러기라 이미지 로드만 늘린다.
- **액세서리는 두꺼운 디자인만** — 얇은 철사테(2px)는 차분 추출 침식에 소멸(구 안경 셀당 25px 파편). 뿔테급 두께로 생성.
- **액세서리 레이어 측정은 first-block** — 최대 블록은 추출 고스트를 잡는다(P0-1 1차 시도가 에셋 파손). fit-head 가 내부 처리하므로 수동 측정 시에만 주의.
- 모자=--extract-fine / 안경=기본 추출 비대칭은 실측 결과(fine 이 안경에선 눈 재음영 고스트를 물어옴) — build_assets.sh 주석 참조.
- **align.json(row_dy=[0,4,10])은 마네킹 세대 고유값** — 마네킹 재생성 금지.
- 차분 추출 시 아이템 색이 마네킹 표면색(살구 피부·흰 탱크톱·회색 반바지)과 비슷하면 구멍.
- 크로마키 그린 #00FF00 + "방송용 그린스크린" 문구. 초록 계열 아이템은 마젠타 키 전환 필요(미구현).
- 분리옷 추출 단계의 `QA: FAIL`은 전신 지표를 부분 레이어에 적용한 노이즈 — 착용 시트 `--metrics-only` PASS + 합성 육안이 실제 게이트.
- `*.log`는 gitignore — 프롬프트 .md와 raw PNG만 커밋.
- `_mannequin.png` **단독** 렌더 경로 금지 — 분리옷 바닥 레이어로만(sanitize가 top+bottom 완비 보장).
- world.js는 IIFE 본문에서 buildPicker()가 즉시 실행 — 피커가 만지는 `let`은 선언을 위에(TDZ 전례 2회).
- 카탈로그는 클라/서버 2벌 미러 — 한쪽만 고치면 "저장했는데 남에게 기본 코디로 보임".
- wrangler dev는 `--assets` 없이 워커만 띄우면 reload 루프 없이 WS 검증 가능(스크립트는 저장소 밖에).
- 어떤 게임도 발사 URL의 characterId를 읽지 않음(자체 선택/서버 배정) — 말랑 친구 사전 선택 UI를 되살리지 말 것(설계서 §2).
