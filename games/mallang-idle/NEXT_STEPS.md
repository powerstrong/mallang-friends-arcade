# 말랑프렌즈 키우기 — 현재 상태와 다음 작업

`mallang-idle` · 최종 갱신 2026-08-17

> 새 세션은 이 문서 → [AGENT_PROTOCOL.md](AGENT_PROTOCOL.md) 순으로 읽고 이어간다.

---

## 지금 어디까지 왔나

**상태: `v1 candidate+` — 사람 게이트 대기.** 상태는 네 단계로 구분한다(codex 리뷰):

| 단계 | 상태 |
|---|---|
| 구현 | ✅ P0~P5 전 범위 + **2막(챕터 6·7)·6번째 캐릭터** + **전투 화면 개편 단계 0~5 + 에셋 실장** (2026-08-15~17) |
| 기계적 검증 | ✅ 정적 회귀 67개 + **실브라우저 회귀 124개**(CDP 실좌표 클릭) · 시뮬 지표 · 시각 스모크 |
| **사람 게이트** | ⏳ **대기** — P1 "5분 뒤 한 번 더 강화하고 싶은가"는 사람만 판정 가능 |
| 배포 검증 | ⏳ 8차 이후 수정분 재배포 후 신규 사용자 플로우 재확인 필요 |

사람 게이트 통과 전에는 POST-v1(서버·랭킹·광장 경제)을 시작하지 않는다.

### ▶ 현재 작업 트랙 — 전투 화면 대개편 (**구현 완료 · 사람 게이트 대기**)

사용자 판정: *"전투가 수평에 한 대 치는 모션뿐, 토끼는 걷지도 않는다. 매력이 없다."*
→ **[COMBAT_STAGE_OVERHAUL.md](COMBAT_STAGE_OVERHAUL.md)** 가 이 트랙의 단일 원천이다.
릴리즈 품질 7기둥 + 단계 0~5, 각 단계마다 "완성 아님" 체크리스트로 조기 완성 판정을 막는다.
**단계 0~5 구현이 전부 끝났다** — 남은 것은 사람 게이트(20초 첫인상·실기기 성능).

| 단계 | 상태 |
|---|---|
| 0 — 씬 렌더러 골격 + 연출 이벤트 버퍼 | ✅ **완료** (10차, 커밋 `518d454`) |
| 1 — 캔버스 FX/파티클 (D기둥) | ✅ **완료** (12차, 커밋 4개 `a4a3cfc`~`2070dc2`) |
| 2 — 이동감: 다층 시차 + 걷기/달리기 (A기둥) | ✅ **완료** (13차) + **에셋 부채 해소** (17차) |
| 3 — 전투 안무: 캐릭터별 공격·스킬 (B기둥) | ✅ **완료** (14차) + **에셋 부채 해소** (17차) |
| 4 — 카메라 & 보스 개성 (C·E기둥) | ✅ **완료** (15차 — 줌·팬·펀치 + 보스 컷인/분노 페이즈) |
| 5 — 오디오 싱크 · 접근성 · 성능 마감 (F·G기둥) | ✅ **구현 완료** (16차 — 오디오·접근성 완료, 성능은 CI 초록·실기기는 사람) |
| 에셋 실장 — 시트 24장 + 시차 아트 9장 (imagegen) | ✅ **완료** (17차 — 원격 세션이 못 한 codex 위탁분) |

**개편 트랙의 에이전트 작업은 전부 끝났다(에셋 포함).** 남은 것은 **사람 게이트**다 —
20초 첫인상, 실기기 성능(결정 6②), 7기둥 종합(COMBAT_STAGE_OVERHAUL.md 10절).
엔진(`engine/*` · `data/*` 수치)은 이 트랙 내내 **불변**이었다 — 연출은 전부 표현 계층에서.

### 2026-08-17 (18차) — 던전을 "하는 콘텐츠"로: 별빛 시련 리플레이 의식

사용자 판정: *"도전은 뭔가 컨텐츠를 하는 느낌이 있어야 하는데 그냥 누르자마자
클리어! 뜨면 재미가 없다. 아예 필드가 바뀌고 3 2 1 start! 하는 성의는 필요하다."*
— 구버전 주석("달리는 척은 시간 낭비다 — 장르 표준")이 정확히 뒤집혔다. **성의
3요소**(진입 전환·시작 신호·과정 가시화)를 원칙으로 채택(메모리에도 기록).

- **흐름**: 입장 → 보상·횟수 **즉시 지급**(진실원 유지 — 이탈해도 유실·복제 없음) →
  베일 전환 → **필드가 시련의 무대로**(신규 `bg-dungeon-star.jpg` + 기존 starsea
  far/sky/fg 재사용) → **3·2·1·START!**(전용 스팅 count/go 신규) → **보스 러시 재생**
  (엔진 `Dungeon.simulate` 의 log 를 그대로 — 챕터 보스들이 순서대로, 연승 칩·HP
  드레인·타격 안무·격파 세트피스·⭐플로트) → 못 잡는 보스는 **격파 없이 위협**으로
  끝(정직한 재생) → 결과 배너("N연승!") → 베일 복원 → 결과 팝업
- **구조**: 전부 표현 계층 `render/stage.js` 의 상태기계(`dungeonRun/dungeonSkip/
  dungeonActive`). 엔진 무변경 — log·보상 수치가 진실원이고 화면은 재생만.
  리플레이 동안 본편 큐 소비 정지(엔진은 계속 돎), 종료 후 큐 가속이 밀림 흡수.
  본편 보스 타이머 HUD 는 리플레이 중 억제(복귀 프레임 강제 재평가)
- **탭 = 스킵**: 의식은 남기고 반복 피로만 접는다(의식 제거가 아니라). 감속 모드는
  카운트다운·배너(정보) 유지 + 움직임만 제거
- **회귀가 잡은 실버그**: 복원 시 `setChapter` 가 자기 던전 가드에 막혀 필드가 안
  돌아오는 버그 — `applyChapterField` 분리로 해소
- **검증**: 정적 67 + 실브라우저 **124**(stage **108** = 기존 95 + 던전 13) 전부 초록
  + 시각 스모크 4컷(카운트다운·러시·배너·복원) 육안 확인
- 다음: 이 "의식" 패턴을 다른 도전 콘텐츠에도 적용(보스 재도전·향후 이벤트).
  자기 판단 개선 계속 — "할 수 있는 건 다 했다" 프레임 폐기(사용자 지시)

### 2026-08-17 (17차) — 에셋 부채 전량 해소: 시트 24장 + 시차 아트 9장 실장

원격 세션(14~16차)이 codex 부재로 못 한 **imagegen 위탁분**을 로컬에서 처리했다.
33종 생성(병렬 레인) → 선별(불합격 6종 재생성) → 가공 → 실장 → 회귀·시각 검증.

- **캐릭터 시트 24장**: 6명 전원 걷기 6f(`hero-*-walk.png` 교체) · 달리기 6f(신규
  `hero-*-run.png`) · 기본공격 6f(신규 `atk-*-sheet.png` — 캐릭터별 안무 그림:
  모찌 내려찍기·피치 2연쪼기·푸딩 구르기·라떼 도약·민트 대시·별사탕 꼬리 튕김) ·
  스킬 세트피스 8f(신규 `atk-*-sp.png`). `characters.js` 에 시트 메타(walkN·runN·
  atkSheet·atkSp) 추가 — 스킬 **수치 무변경**
- **시차 아트 9장**: far 7챕터(`far-*.png`, `FAR_ART`) + sky 2종(주간 구름·별하늘,
  `SKY_ART` — 야외 챕터만, 실내는 기존 감쇠). CSS 그라디언트 플레이스홀더는 폴백으로 유지
- **재생 배선**: 걷기/달리기 시트는 `applySprite` gait 스왑 + `frames-6` 클래스,
  공격은 `swapToAttackPose(isSkill)` + CSS `atkFlip` 플립북(`atk-6`/`atk-8`).
  **steps(var()) 는 Chrome 이 셔한드에서 소비 못 한다(실측)** — 고정 클래스 변형으로 확정
- **감속 모드 잠재 간극 봉합**: 모션 클래스가 `animation ... !important` 라 감속 블록
  평서술이 지고 있었다 → 블록에 `!important` 부여(타격 모션·플립북도 정지)
- **가공 파이프라인 교훈(재사용 가치)**: ① 전역 despill 은 민트·초록 본색을 회색으로
  죽인다 → **테두리 연결 플러드필 키잉**(스크린색 실측 → 경계 BFS → 1px 링만 un-blend)
  으로 재작성 ② 픽셀 임계 크로마는 민트 몸통(g 우세)을 반투명으로 오판한다 —
  플러드필이 정답 ③ codex 는 레퍼런스를 첨부해도 색을 놓친다(민트→회색 2회) —
  **색을 프롬프트 첫 줄에 명시** ④ "자체 품질 루프(ralph) 금지 + image_gen 1회" 문구
  없으면 생성이 3배 느려진다 ⑤ 스크립트 원형은 세션 scratchpad `mi-assets/`
  (process_sheets.py — 플러드필 키잉·성분 군집·행 정렬·타일 심리스)
- **검증**: 정적 **67**(balance 36 · ui-contract **13**(+시트 규격 계약) · queue 18) +
  실브라우저 **111**(stage **95**(+에셋 시트 7) · first-visit 10 · multi-tab 6) 전부
  초록 + 시각 스모크 4컷(들판 걷기/타격·별바다·달공장) 육안 확인 — 구름·원경 아트
  실렌더, 공격 시트 플립북 접점 정확, 레이아웃 무결
- 다음: **사람 게이트** (아래 완성도 평가 참조)

### 2026-08-17 (16차) — 전투 화면 개편 **단계 5 구현 완료**: 오디오 싱크·접근성·성능

개편 트랙의 **마지막 단계**. 에이전트가 할 수 있는 두 항목(오디오·접근성)은 완료하고,
성능은 CI 기계 게이트 + 구조적 보호까지 세운 뒤 **실기기 발열/저사양 최종 판정만
사람 게이트로 넘겼다**(결정 6②). 이로써 트랙 구현이 전부 끝났다.

- **오디오 싱크**(G기둥): 안무 사운드를 **연출 계층으로 이관** — 화면과 같은 순간에
  운다(엔진 사건 시점이 아니라). 스킬 발동 `skill`(신규)·보스 포효 `bossIn`·돌파
  `clear`·시간초과 `fail`은 무대 비트/접점이, 분노 진입 `rage`(신규)는 프레이밍이
  낸다. `game.js consumeEvents` 는 로직 사운드(강화·해금)만 즉시. `audio.js` 는
  후크(skill·rage)만 추가(코어 절차 트랙 구조 유지)
- **접근성 마감**(F기둥): 감속 모드가 "정보 유지 + 움직임만 제거"임을 전 경로 확정 —
  데미지 숫자는 뜨되 정적, 분노 긴장 틴트는 남되 맥동만 꺼짐, HP·보스 타이머 갱신,
  카메라 정지. 단계 3·4 새 클래스(공격 모션·적 반응·분노 틴트) 전부 감속 블록 편입
- **성능**: 안무 스킬 FX·카메라 추가 뒤에도 최대 부하 절사평균 **1.03ms · p95 2.50ms**
  (예산 8/16ms) 유지. `INFO_RESERVE`·`PARTICLE_CAP`·탭 숨김 rAF 정지 유효. **5분 발열·
  저사양 실기기 최종 판정은 사람 게이트**(결정 6②) — 에이전트가 [x] 못 하는 항목
- **검증**: 정적 66(무변경) + 실브라우저 **109**(stage 87 = 기존 77 + **단계 5 신규 10** ·
  first-visit 10 · multi-tab 6) 전부 초록
- **다음: 개발 단계가 아니라 사람 게이트** — 20초 첫인상·실기기 성능·7기둥 종합.
  통과하면 트랙 종료 → P1 사람 게이트로 복귀(그 전엔 POST-v1 미착수)

### 2026-08-17 (15차) — 전투 화면 개편 **단계 4 완료**: 카메라 & 보스 개성

*"카메라가 죽어 있고 보스가 큰 몹+타이머 바뿐"* 의 해소. 전부 표현 계층 —
`.stage-cam` transform 만 만지고(HUD 제외) 엔진은 `state.bossT`·타이머를 읽기만 한다.

- **카메라 줌·팬·펀치**: `camera` 가 흔들림만 하던 것을 줌(`scale`)·팬(`panX/Y`)·
  펀치(`punch`)로 확장. 목표를 향한 **프레임레이트 독립 지수 감쇠**라 배속·프레임
  드랍에서도 진폭이 안 튄다. `fxCanvas` 가 `.stage-cam` 안이라 줌과 함께 스케일되고
  좌표 수학이 `camera.scale` 로 되돌린다
- **보스 컷인**(체크리스트 1): `showEnemy(boss)` 가 무대를 확 당겨 잡고(컷인 줌) 홀드
  후 전투 프레이밍(1.08)으로 정착 + `.boss-mode`. 착지에 줌 스냅 펀치
- **막타·돌파 반응**(체크리스트 2): `boss_clear` 가 줌 펀치+셰이크로 반응하고 짧게
  붙었다 1.0 으로 감쇠
- **분노 페이즈**(체크리스트 3): 보스 타이머 62% 경과에서 붉은 비네트 맥동
  (`.boss-rage`) + 카메라가 더 조인다(1.08→1.18). 화면 긴장이 제한시간과 함께 오른다
- **멀미 하드 가드**(체크리스트 4): 세 축 상한(scale ≤ 1.24·pan ≤ 30px·shake ≤ 7·
  punch ≤ 0.14)으로 어떤 입력도 클램프. 감속 모드는 카메라 전면 정지(줌·팬·흔들림 0)
- **좌표 회귀 견고화**: `enemyPoint`(월드좌표)와 `getBoundingClientRect`(스크린좌표)
  비교를 카메라 scale 로 나눠 보스 줌에서도 117px 회귀를 그대로 잡게 갱신
- **검증**: 정적 66(무변경) + 실브라우저 **93**(stage 77 = 기존 65 + **단계 4 신규 12** ·
  first-visit 10 · multi-tab 6) 전부 초록 + 보스 분노전 컷 육안 확인(줌 1.175·HUD
  타이머·필요DPS 안 흔들리고 읽힘·분노 틴트·막타 FX)
- **남긴 부채**: 막타 슬로모는 DOM CSS 를 못 늦춰 줌 펀치로 갈음(액터 캔버스 승격 시
  재론). 에셋 부채(다중 프레임 시트)는 여전히 imagegen 위탁분
- 다음: **단계 5 — 오디오 싱크·접근성·성능 마감** (트랙 마지막, 10절 갱신됨)

### 2026-08-17 (14차) — 전투 화면 개편 **단계 3 완료**: 전투 안무 (캐릭터별 공격·스킬)

*"한 대 치는 모션뿐"* 의 구조적 해소. `heroStrike` 단일 돌진 반복을 걷어냈다.
전부 표현 계층 — 엔진·`characters.js` 스킬 **수치** 무변경(스킬의 '그림'만 새로 정의).

- **캐릭터별 안무 6종**(체크리스트 1): 스킬 축(`skill.key`)마다 고유 모션 —
  모찌 강타 `st-slam` · 피치 연타 `st-combo` · 푸딩 회전 `st-spin` · 라떼 도약 강타
  `st-leap` · 민트 대시 `st-dash` · 별사탕 튕김 `st-flick`. `STRIKE_STYLES`+
  `STYLE_BY_SKILL`(stage.js)이 클래스·접점 타이밍·돌진 배수·정체성 색을 소유
- **리듬 3계층으로 반복감 제거**(체크리스트 2): 기본 **교대타**(스타일 모션 ↔ 빠른
  잽 `jab`을 한 번 걸러) + 매 5타 강조 + 매 9타 스킬 세트피스. 한 캐릭터 안에서도
  20타에 시그니처 ≥3종. 발동 주기는 순수 표현 리듬 — 엔진 DPS 와 무관
- **스킬 = 편성 정체성 FX**(체크리스트 3): `heroSkillFx`가 `skill.key`로 분기 —
  모찌 충격파 · 피치 연타 스파크 · 푸딩 골드 분수 · 라떼 도약 충격파+셰이크 · 민트
  수평 대시 잔상 · 별사탕 별가루 샤워. 전부 장식이라 감속 모드에서 자동 억제
- **적 피격 반응 다양화**(체크리스트 4): `reactClass`가 타격 크기·교대·보스로 4종 —
  잔타 `squash`/`squash-spin`(교대) · 큰 한 방 `squash-heavy`(넉백 `--knock` 비례) ·
  보스 `squash-boss`(덜 밀리고 번쩍). 히트스톱·카메라 셰이크는 강조·스킬에서 강화
- **검증**: 정적 66(무변경) + 실브라우저 **81**(stage 65 = 기존 58 + **단계 3 신규 7** ·
  first-visit 10 · multi-tab 6) 전부 초록 + 전투 컷 육안 확인(모찌 강타 포즈·별가루
  FX·타격 크기 반응 "900" huge·다층 시차 무결)
- **남긴 부채**(에셋 배치, 단계 2와 동일 성격): 다중 프레임 시트(attack_basic 5~8·
  attack_special 8~12)는 imagegen 위탁분 — 지금은 단일 공격 포즈 위에 모션 변주로
  시스템 완성("에셋 대기로 정체 금지"). 시트가 오면 `applySprite` 프레임 스왑만 추가.
  "10초 첫인상" 최종 체감은 사람 게이트
- 다음: **단계 4 — 카메라 & 보스 개성** (COMBAT_STAGE_OVERHAUL.md 10절 갱신됨)

### 2026-08-17 (13차) — 전투 화면 개편 **단계 2 완료**: 이동감 (`9390477`)

*"토끼가 걷지 않는다"* 의 구조적 해소. 전부 표현 계층 — 엔진·데이터 무변경.

- **전진 beat**: mob_kill 0.55→0.95 · boss_clear 1.05 — 처치 후 다음 몹까지
  걷는 구간이 화면에 실존한다. 배속·복귀 밀림은 단계 0 큐 가속이 그대로 흡수
- **시차 4겹**(0.22/0.5/1.0/1.8): 구름·원경 실루엣 신규 — CSS 플레이스홀더
  (radial-gradient 스캘럽) + 챕터 무드 틴트(FAR_TINT) + 실내 구간 구름 감쇠.
  전용 아트 없이 입체감이 선다 (에셋 대기로 정체 금지 원칙)
- **걷기/달리기 구분**: 큐 따라잡기(rate>1.25) 구간 = 달리기 — 걸음 주기
  0.48→0.3s·전경 기울임·발밑 먼지 궤적·시차 1.7배. 따라잡으면 걷기 복귀.
  게임 상태와 맞물린 구분이라 장식이자 정보다. 동료 위상차는 주행에도 별도
- **검증**: 정적 66(balance 36·ui-contract 12·queue 18) + 실브라우저 74(first-visit
  10·multi-tab 6·stage 58) = **140개 전부 초록** + 전진 컷 육안 확인(실루엣이
  원화와 자연스럽게 겹침)
- **부채**(단계 3 에셋 배치에 통합): 걷기/달리기 6~8프레임 시트, 시차 전용 아트,
  "볼 만한 길이" 최종 체감은 사람 게이트
- 다음: **단계 3 — 전투 안무** (imagegen 에셋 위탁 선행, 10절 갱신됨)

### 2026-08-17 (12차) — 전투 화면 개편 **단계 1 완료**: 캔버스 FX/파티클 시스템

10절 순서(기반 → 테스트 먼저 → 수직 슬라이스, 슬라이스마다 성능 측정)를 그대로 밟았다.
커밋 4개, 매 슬라이스 회귀 전부 초록 상태로 적재.

- **기반**(`a4a3cfc`): 세계좌표 = fx-canvas 로컬로 통일(117px 좌표 버그 수정 — DOM 은
  spawnFx 가 레이어 변환), stage FX 시계(히트스톱은 **파티클 하위 시계만** 동결, 중첩은
  긴 쪽, DOM 정지 클래스 동기), 정보/장식 이원화(감속 = 장식 0 + 정보 저동작, CAP
  포화 = 장식부터 거절 · INFO_RESERVE 48/400), 시트 캐시 + decode 추적.
  `queue.js` 에 onIdle(busy→idle 전이 정확히 1회, 메모리 상한 경로 포함) 신설
- **슬라이스 1**(`1d4eeee`): 임팩트 = 플립북 drawImage + 검격 궤적 + 방사 스파크
  (가산 발광·잔상) + 섬광 합성. **데미지 숫자 진실원 = 관측 HP 델타**(결정 8 —
  DPS 공식 재계산 폐지, crit→accent 개명), 크기/색이 타격 크기에 반응(big/huge)
- **슬라이스 2**(`8fc4bf7`): 처치 = poof 플립북 + 구름 파편 + 골드 분출 + 섬광
  세트피스. **collapsed 합산 골드 queue idle 플러시**(벽·보스 대기 중 증발 결함 봉인),
  골드 플로트 FX_CAP 편입, 합성 순서 z 계약(정보 DOM > 장식 캔버스)
- **슬라이스 3**(`2070dc2`): 별 투사체 WAAPI → 캔버스 탄도(수명 종료 = 착탄, 히트스톱이
  궤적도 동결). 장식 DOM FX 전폐(.fx-impact/.fx-proj/.fx-slash/.fx-poof CSS 제거) —
  DOM 잔존은 정보(.fx-dmg·.gold-float)뿐. ui-contract 이관(완화 아님 — 상한·소유권·
  z 순서·플러시 배선 계약 추가)
- **성능 CI 게이트**: 혼합 최대부하(시트 60 + 가산·스트레치 스파크 + 정보 48 =
  CAP 400 포화) **절사평균 0.56ms · p95 2.20ms** (예산 8/16ms — 매 실행 실측 출력).
  모바일 60fps 판정은 단계 5 실기기 게이트로 명시 이관
- **검증**: 정적 65(balance 36 · ui-contract 11 · queue 18) + 실브라우저 69(first-visit
  10 · multi-tab 6 · stage 53) = **134개 전부 초록** + 시각 스모크 4컷(타격/처치/보스/
  투사체) 육안 확인 — 접점 정확·숫자 가독·레이아웃 무결
- 다음: **단계 2 — 이동감** (COMBAT_STAGE_OVERHAUL.md 10절 갱신됨)

### 2026-08-17 (11차) — 단계 1 착수 전 계획 보강 + codex 계획 리뷰 반영 (문서만, 코드 불변)

단계 1 계획을 코드 실물과 대조해 빈틈을 메우고, codex 리뷰(판정: 보류)를 받아 차단점을
계획에 선반영했다. **구현 착수 전에 잡았으므로 전부 문서 수정으로 끝났다.**

- **보강 1차(자체)**: 히트스톱이 DOM 전용(`animation-play-state`)이라 캔버스 이관 시
  타격감이 빠지는 문제, reduced-motion 의 `emit()` 전면 차단이 정보 FX 까지 지우는 문제,
  collapsed/CAP 우선순위·성능 측정 근거 부재를 실패 조건으로 추가하고 설계 결정을 신설
- **codex 계획 리뷰 반영(차단점 3)**: ① `.fx-layer`(field 안)와 `.fx-canvas`(stage-cam
  직속)의 **좌표계가 이미 어긋나 있다**(실측 117.5px — 단계 0 먼지가 현재 이 버그를
  가짐) ② 데미지 숫자의 `Combat.dps×simDt` 누적 + `effAspd` 타이밍은 **표현 계층의
  전투 공식 재계산** = 3.1절 원칙 충돌 → **관측 HP 델타로 교체 확정**(결정 8, `crit`→
  `accent` 개명) ③ 평균 8ms/p95 16ms 는 headless·`--disable-gpu` 하니스에서 "모바일
  60fps"의 증거가 못 됨 → **CI 회귀 게이트(단계 1)와 실기기 완료 게이트(단계 5)로 분리**
- **codex 추가 발견 반영**: collapsed 로 배치가 끝나면 `floatAccum` 합산 골드가 영영
  안 보임(queue idle 플러시 조건 신설), 감속 CSS 블록에 `.fx-dmg`·`.gold-float`·
  `.boss-ring` 부재(정보는 "유지+정적 대체"로 명문화), `.fx-layer`/`.fx-canvas`
  z-index 3 동률로 장식 캔버스가 숫자를 덮을 수 있음(합성 순서 확정), 골드 플로트가
  `FX_CAP` 밖(잔존 DOM 상한 신설), `fx-slash` 는 시트가 아니라 단일 이미지+변형,
  이미지 decode·수명 조건 신설, 정적 계약(ui-contract)과 동작 검증(stage.browser)의
  역할 분리
- 결과: 단계 1 실패 조건 4개 → **13개**, 착수 전 설계 결정 **8개** 확정, 10절 착수
  순서를 "기반 → 테스트 먼저 → 수직 슬라이스, 슬라이스마다 성능 측정"으로 재작성.
  리뷰 원문: `.omc/artifacts/ask/codex-…2026-08-17T00-08-11….md` (세션 산출물)
- 문서 정리: 회귀 테스트 수 정합(정적 60 + 실브라우저 35 — 표·명령 블록이 8차 수치에
  머물러 있었다), 이미 8차에 구현된 "시뮬 오프라인 모델 검토" 백로그 항목 제거
- 검증: balance 36 · ui-contract 10 · queue 14 전부 초록 재확인 (코드 무변경)
- 다음: 10절 순서 1 — **기반(좌표 통일·decode·FX 시계·정책표)**부터. 첫 커밋은
  "117px 좌표 버그 수정 + 좌표 일치 브라우저 테스트"가 자연스러운 출발점

### 2026-08-16 (10차) — 전투 화면 개편 **단계 0 완료**: 씬 렌더러 + 연출 버퍼

기획(9차)의 단계 0을 구현했다. 이 개편의 **심장 부품**이 섰다.

- **`render/queue.js` 신설 — 연출 이벤트 버퍼.** 엔진의 "순간 확정"과 화면의 "볼 만한
  페이스"를 분리한다. 각 사건에 최소 화면 시간을 주고, 밀리면 가속(soft 1.2s → 최대 x10),
  폭주하면 몰아보기(48개/8초/메모리 4096). **불변조건: 사건 유실 0** — 몰아보기에서도
  전부 전달하고 `collapsed` 플래그로 연출 강도만 낮춘다. 밸런스에는 영향 없음
- **`render/stage.js` 신설 — 전투 무대를 통째로 소유.** 시차·액터·카메라·캔버스 파티클
  4개 하위 시스템 골격. `game.js`에서 아레나 렌더 전부 이관(-370줄): `renderArena`
  `heroStrikeFx` `spawnFx` `applySprite` `syncLunge` `updateAmbient` `floatGold` 등
- **핵심 불변조건 `queue.idle() → syncTo(state)`** — 연출이 끝나면 무대가 엔진 진실로
  스냅한다. 부팅·컷신 복귀·오프라인 복귀·dev 점프에서 영구 어긋남이 생기지 않는다
- **사건 처리 이원화**: 무대(연출)는 큐를 타고, 로직(도감·해금·컷신·패널·BGM)은 즉시.
  도감 기록을 스프라이트가 아니라 **사건**에서 하도록 옮겼다 — 몰아보기로 건너뛴 몹이
  도감에서 누락되던 잠재 결함을 이관 과정에서 차단
- **카메라 도입**: `.stage-cam`이 무대만 감싼다(HUD 제외) — 화면이 흔들려도 보스 타이머·
  진행도는 안 떨린다. CSS 키프레임 셰이크(`.arena.shake`/`microshake`) 폐지, JS 감쇠로 대체
- **캔버스 파티클 레이어**: DPR 인식 사이징 + 풀. 파이프라인 증명으로 발밑 먼지를 먼저
  옮겼다(`.fx-dust` CSS 제거). 나머지 흡수는 단계 1
- **테스트 +33개**: `queue.test.js` 14(실제 엔진으로 x1/x5/x20·1시간 버스트 구동),
  `stage.browser.test.js` 19(무대-엔진 동기·유실 0·DPR·감속 모드 별도 기동).
  `ui-contract`에 "무대 렌더가 game.js로 되살아나면 실패" 계약 추가
- **버그 2건**(테스트가 잡음): ① 큐 `drainAll`의 반복 상한이 `maxQueue*2`로 묶여 대량
  투입 시 나머지를 조용히 버림(5000개 중 4800개 유실 — 불변조건 위반) → 스냅숏 후 배출로
  수정 ② stage.js UMD가 팩토리에 `root`를 안 넘겨 부팅 실패
- **하니스 개선**: 리눅스/컨테이너 브라우저 경로 자동 탐색 + root일 때 `--no-sandbox` +
  추가 기동 인자 지원. **실브라우저 회귀를 이 환경에서 실제로 돌릴 수 있게 됐다**
- 검증: balance 36 · ui-contract 10 · queue 14 · first-visit 10 · multi-tab 6 ·
  stage 19 = **95개 전부 초록** + 전투/보스 스크린샷 육안 확인(레이아웃 무결)
- 다음: **단계 1 — 캔버스 FX/파티클**로 DOM 이펙트 26개 상한을 걷어낸다

### 2026-08-16 (9차) — 전투 화면 대개편 기획 (구현 전, 문서만)

사용자 지적: *"전투가 수평에 한 대 치는 모션뿐, 토끼는 걷지도 않는다. 매력이 없다."*
정직한 진단 결과 **파라미터 튜닝으로는 못 고친다** — 구조 문제다. 근본 원인 3종:
① 전진이 `advanceSeconds 0.6`초뿐이라 화면의 95%가 정지, ② 공격이 `heroStrike`
키프레임 하나의 영원한 반복, ③ 표현 계층에 자기 시계·연출 큐가 없어 "엔진의 순간 계산"을
그대로 그린다. → **[COMBAT_STAGE_OVERHAUL.md](COMBAT_STAGE_OVERHAUL.md)** 신설.

- **범위 확정**: 엔진(`combat/balance/save/dungeon` + `data/*` + 메타 UI)은 **불변**,
  아레나 렌더 내부(`renderArena/heroStrikeFx/spawnFx/syncLunge` + `style.css` 아레나
  구획 + 3프레임 단일공격 모델)는 **갈아엎는다**
- **아키텍처 결정**: DOM 액터(상태 기반 다중 프레임) + 캔버스 FX/파티클 **하이브리드**
  (풀 캔버스는 접근성·실브라우저 회귀·무빌드 자산을 이유 없이 버려서 기각)
- **심장 부품**: 연출 이벤트 버퍼 — 엔진 순간 확정과 "볼 만한 페이스"를 분리(catch-up 포함)
- **7기둥 릴리즈 기준** + 단계 0~5마다 **"완성 아님" 체크리스트**를 박아 조기 완성 판정 차단
- **에셋 스펙 선확정**(이미지는 나중에): 캐릭터 6상태·적/보스 4상태·시차 5겹·FX 아틀라스
- 다음 세션 착수: `render/stage.js` 골격 + 연출 버퍼 + 배속/복귀 catch-up 회귀부터

### 2026-08-16 (8차) — codex 재미 평가 반영 + 2막 콘텐츠 + 정직한 시뮬

codex 재미 평가(TOP 10, scratchpad review — 요지는 커밋 메시지들에 남음)를 받아
즉효 항목을 반영하고, 콘텐츠 지평선을 2막으로 확장했다. 커밋 6개:

- **폴리시 4건**(6847df8): BGM 크로스페이드(전용 게인 버스, 0.12s 딥→0.3s 페이드인),
  음소거 시 BGM interval 해제, 던전 카드 사전 정보("N층 기준 · 예상 K연승 ⭐M" —
  결정론이라 예상=정답), 타격 접점 통일(돌진 거리 매 타격 실측, 캐릭터 폭 차 흡수)
- **리뷰 즉효 4건**(c726eaa): ASPD 실효 상한 도달 시 구매 차단+"최대 도달"(골드
  낭비 방지), 보스 데미지 숫자에 나침반 포함(bossDps 로 일치)+가짜 크리 '!' 제거,
  오프라인 팝업에 "지금 강화 +N레벨 가능!" CTA, 탭 점진 공개(편성=첫 동료·유물=
  첫 조각·도전=해금 10·도감=수집 3종)
- **시뮬 오프라인 세션 모델**(175c1e5): `--sessions=15/120`. 24h 측정 —
  연속(도달 210·벽 67m·체류 82.6%) vs 세션(도달 169·**활동 기준 벽 6m30s·체류
  3.6%**·오프라인 골드 70.1%). "연속 시뮬은 비관적"이라는 가설이 측정으로 확인됨
- **2막 개막**(59f1a19): 정원을 181~210으로 접고 **챕터 6 '별빛 바다'(211~270,
  별똥 아기·달빛 해파리·꼬마 혜성·보스 별고래) · 챕터 7 '고장난 달 공장'(271~∞,
  부서진 정찰기·나사 요정·달먼지 뭉치·보스 달의 감독관)**. "별조각과 기계군단은
  어디서 왔나"가 새 훅 — 무한 꼬리 챕터를 서사가 설명한다("공장은 멈추지 않는다").
  **6번째 캐릭터 '별사탕 수달'**(해금 120, 구출 서사, 별조각 +35% = shardMul 축 —
  벽에서 "라떼(즉시 돌파)냐 별사탕(유물 성장)이냐"의 새 갈림길). BGM 무드 2종.
  에셋 16종 = codex imagegen 30장 대량 생성 → 선별 → un-blend 가공
- **밸런스 2건**(b0a7f58): **의도된 첫 벽**(firstWallStage 5 ×1.3 — 실패 4m06s →
  벽 46.5s → 4m53s 돌파, "막힘→강화→돌파"가 첫 5분 안에 완성) + **해금 재배치**
  (푸딩 22·라떼 55·민트 90·수달 120 — 30분 전부 소진 문제 해소, 첫 주 내내 복귀
  목표 유지). 편성 패널에 "다음 친구는 스테이지 N" 예고
- 검증: balance 36 · ui-contract 8 · first-visit 10 · multi-tab 6 + 실브라우저
  시각 스모크(별바다 전투/보스전, 달공장 전투/보스전 — 수달 리드 공격 포즈 포함)

### 2026-08-16 (7차) — codex REQUEST CHANGES 반영 + 병아리 스프라이트 정정

- **[P0] 첫 방문 시작 회귀 수정**: 부팅 렌더(renderHud)가 DOM 텍스트 비교로 챕터
  전환을 감지하다 인트로(z-50) 위로 컷신(z-65)을 띄워 시작 버튼이 눌리지 않던 회귀.
  `lastChapterId` + `applyChapterVisuals`(배경·전경만)로 시각 적용과 전환 연출
  (배너·컷신·BGM)을 분리 — 부팅 렌더는 이제 부수효과가 없다. `nextSceneFromQueue`
  에 seen 가드(중복 재생 방지), 구출 재큐는 인트로 뒤로 이동
- **실브라우저 회귀 테스트 신설** — `tests/browser-harness.js` (무의존: 헤드리스
  Chrome/Edge + CDP + Node 22 내장 WebSocket, 임시 프로필 = 항상 새 origin).
  `first-visit.browser.test.js` 10개: computed style·elementFromPoint·실좌표 클릭으로
  위 회귀를 그대로 고정. `multi-tab.browser.test.js` 6개: 멀티탭 정지→이어하기 검증.
  소스 문자열만 보는 정적 테스트가 못 잡던 부류가 이제 저장소 안에서 재현된다
- **[사용자 버그] 병아리가 강아지로 보임**: `hero-chick-walk.png` 파일 자체가 푸들
  그림이었다(공격 포즈·초상화는 정상). 원본(jump-climber의 "피치 병아리 워크
  스프라이트 시트.png")부터 오염 — imagegen 재생성으로 정정. 마젠타 요청은 두 번
  연속 검정+후광으로 실패, **"3x3 그리드 + 방송용 그린스크린 #00FF00 + 글로우 금지"
  프롬프트가 성공** (다음 에셋 작업 시 참고). 가운데 줄(옆모습) 추출 → 조각 제거 →
  바닥 정렬 재조립, frameW 165→188
- **[사용자 질문] "필드에 한 명만 나오나?"**: 편성 전원(최대 3명)이 함께 서는 구조는
  원래대로이고 슬롯이 1/10/40 에서 열릴 뿐 — 이걸 UI 가 말하지 않아 오해가 생겼다.
  편성 패널에 "최대 3명 함께 싸워요 · 다음 자리 스테이지 N" 힌트 상시 표시
- **[사용자 요청] 모션 역동성**: 걷기 수직 밥+기울임(동료는 위상차), 타격에 예비
  동작(움츠림)→돌진(접촉 120ms, CSS 40% 키프레임과 동기), 돌진 발밑 먼지, 피격
  넉백 강화(떠올랐다 낙하+회전), 지원 사격 점프 강화, 보스 착지 먼지(fx-dust).
  전부 표현 계층 — 엔진 불변, reduced-motion 존중, FX_CAP 유지
- **[P1] 멀티탭 가드 복구 UX**: 1회성 토스트 → 지속형 모달("다른 탭에서 플레이 중")
  + "최신 저장 불러오기"(reload) + "이 탭에서 이어하기"(lastWroteAt 재기준 → 즉시
  재저장으로 소유권 회수). 정지 즉시 `Audio.bgm(null)`. `storage` 이벤트로 5초
  주기를 기다리지 않고 즉시 감지
- **[P1] 문서 정합**: 24h 벽 기준을 75분(4,500초)으로 재확정하고 BALANCE.md 에
  구조적 하한(~50분) 근거 명시(60분 목표는 오프라인 모델 도입 후 재론), ROADMAP
  유물 계수를 실측치(×1.06/×1.09)로, 본 문서 수치 정정(테스트 41→43, 세이브 v5,
  에셋 58파일)
- **[P2] 시뮬 과제 경제 이원화**: `--model=baseline|upper|both`(기본 both) —
  baseline 은 실제 game.js 와 같은 Quests API 로 달성분만 수령, upper 는 기존
  전액 즉시 수령 상한. 회귀 테스트가 고정하는 기준은 upper(연속성). **벽 체류
  (wallRatio) 지표 신설** — idleRatio 는 67분 벽에서도 0%라 정체를 못 쟀다.
  24h 실측: 두 모델 차이 미미(시뮬 활동량이면 과제를 실제로도 다 채움), 벽 체류 82.6%

### 2026-08-16 (6차) — 자체 검증 루프 2회차: codex 심층 리뷰 반영

1차 심층 리뷰(review4)가 뒤늦게 완주 — HIGH 2 + MED/LOW 다수. 반영:
- **[HIGH] 구출 컷신 영구 유실**: 컷신 도중 새로고침 시 boss_clear(피날레)가 재발화하지
  않아 장면이 사라짐 → 부팅 시 "피날레 지남 + 미시청" 구출 장면 재큐로 복구
- **[HIGH] BGM 복귀 폭주**: 숨김 탭 동안 오디오 클록이 흘러 복귀 순간 밀린 스텝
  수천 개를 한 틱에 예약 → 0.5초 이상 밀리면 재기준화
- fxCount 이중 감소(FX_CAP 무력화) → 멱등 dispose / 타격 좌표를 돌진 시작에 고정 /
  첫 챕터 배경·전경 미적용 수정 / 배너·하트가 컷신에 가려짐 → 컷신 종료 후로 이동 /
  보스 링 중심 보정 / 편성 교체 토스트 / 탭 점 오탭(closest) / 처치음 스로틀 /
  reduced-motion 앰비언트 생성 생략 / 과제·던전 버튼 44px / 던전 결과 "확인"
- 미반영(백로그): BGM 무드 전환 0.3s 크로스페이드, 캐릭터별 타격 접점 통일,
  던전 사전 정보(기준층·예상 보상) 표시, 음소거 시 interval 해제

### 2026-08-16 (5차) — 자체 검증 루프 1회차

- **허공 펀치 제거**: 교전 시 파티가 적 앞까지 접근(.field.engaged 대형, 실측 캘리브레이션
  — 정지 시 몹과 11px). 보스는 몸집만큼 덜 붙는다. 전경 띠 15→10%(발 잠김 수정)
- **걷기 미끄러짐 제거**: 원본 워크시트의 프레임 내 드리프트(26~40px)를 프레임별
  트림+중앙·바닥 정렬 재추출로 해결 (codex 의 프레임 bbox 분석에서 발견)
- **멀티탭 세이브 가드**: 타 탭이 더 새로운 저장을 쓰면 이 탭 일시정지 + 미덮어쓰기
- **검증 부채**: ① 실기기 시각 확인(창 미표시로 스크린샷 불가, 실측으로 갈음)
  ② codex 플레이테스트는 이번엔 브라우저 연결 불가로 무산 — 다음 세션에서 재시도.
  1차 심층 리뷰 로그는 scratchpad/review4_out.md (결론 미작성, 분석 노트만 유효)

### 2026-08-16 (4차) — 스토리 연출·BGM·구출

- **구출 연출**: 기계군단 두 챕터의 피날레(스테이지 130·180) 돌파 시 하트 버스트 +
  구출 컷신. "개조된 친구들이 돌아온다"는 훅이 눈앞에서 완성된다
- **컷신 화자 점프**: 말하는 친구가 화면에 있으면 대사에 맞춰 폴짝
- **절차 생성 BGM**(audio.js): 챕터 무드 4종(들판 펜타토닉·언덕 도리안·기계 단조·
  정원 리디안) + 보스 긴장 트랙. 오디오 클록 룩어헤드 스케줄러라 프레임 드랍에도
  박자 유지. 음소거 연동, 숨은 탭에서 정지. 페이즈/챕터 전환마다 자동 전환
- 노이즈 버퍼 재사용(타격음 GC 압력 제거), 탭 role/aria-selected·음소거 aria-pressed
- 무결성 테스트에 구출 컷신 포함 (43개 전부 통과)

### 2026-08-16 (3차) — 스토리 도입

- `data/story.js` — 방치형 문법의 서사: 챕터 진입 컷신(3~4줄, 탭 진행·건너뛰기,
  게임 시간 정지), 동료 합류 대사, 보스 첫 조우 도발 말풍선(정지 없음), 정원 에필로그
- **핵심 훅**: 기계 병아리·햄스터·퍼피 = 잡혀가 개조된 들판 친구들 → 구출 서사.
  기존 적 아트가 그대로 이야기가 된다
- 챕터 전환 배너(이름+태그라인), 세이브 v5(storySeen), 본 장면은 재생 안 됨
- 무결성 테스트: 전 챕터 컷신·도발 존재, 컷신 화자는 그 챕터 시작 전 합류자만,
  대사 1~5줄·60자 이내(길면 스킵당한다)
- 컷신 오버레이는 z-65 (DEV 패널 60 위) — 검증 중 DEV 패널이 스킵 버튼을 덮는
  사고를 실좌표 탭이 잡아냈다

### 2026-08-16 (2차) — 전투 그래픽 기준 상향: 전용 아트 팩

사용자 기준("전투 그래프의 기준")에 맞춰 imagegen 을 본격 활용한 2차 상향:
- **캐릭터 5명 전용 공격 포즈** (atk-*.png) — 타격 순간 걷기 시트 대신 펀치 포즈로
  스왑. 캐릭터가 실제로 "때리는 그림"이 됐다
- **4프레임 플립북 이펙트** — 임팩트 별폭발·처치 구름펑을 steps(4) 재생
  (fx-impact-sheet / fx-poof-sheet). 단일 프레임 스케일 페이드와는 차원이 다르다
- **검격 궤적**(fx-slash) 이 접촉 프레임에 함께 터진다
- **히트스톱** — 접촉 순간 55ms(크리 90ms) 전원 정지. 타격감의 핵심
- **크리티컬 연출** — 5타마다 대형 임팩트 + 금색 데미지 + 마이크로 셰이크 (표현 전용)
- **접지 그림자**(캐릭터·적) · **보스 오라**(맥동 radial) · **적 아이들 모션**
  (등장 후 계속 꿈틀) · 넉백 강화
- **시차 전경 띠**(fg-meadow/fg-machine, 1.8배 스크롤) + **앰비언트 파티클**
  (들판·정원 꽃잎 / 기계 구간 톱니, 동시 8개 상한)
- **보스 격파 집중선**(fx-lines, screen 블렌드 플래시)
- 전부 표현 계층 — 엔진·시뮬 불변, 테스트 41개 유지

### 2026-08-16 (1차) — 전투 연출 강화 + codex 리뷰 반영

- **살아있는 전투**: 공속 연동 돌진-타격 애니메이션, 타격 임팩트 스타 + 데미지 숫자,
  피격 찌부·번쩍, 처치 시 구름 "펑", 몹 폴짝/보스 쿵 등장 연출, **편성 2·3번이
  화면에 함께 서서 별 투사체로 지원 사격**(폴짝 캐스팅 모션 포함). FX 노드 상한 26
- **시작 게이트**: 시작하기를 누르기 전에는 게임 시간이 흐르지 않는다(t=0·gold=0 보장,
  시작 전 오프라인 지급도 차단). 복귀 유저는 인트로 없이 즉시 시작
- **던전 해금**: 스테이지 10 (이전에는 1스테이지부터 열려 초반 경제 우회)
- **시뮬-UI 경제 통합**: 던전 3회 + 일일 과제 조각을 시뮬에 반영. 그 결과 유물 지수
  인플레가 드러나 재조정(유물 비용 1.45→1.6·효과 8→6%, 던전 골드 30→10초,
  과제 18→11조각). 여유비 2.44 → 1.90(30분)·1.57(2시간)
- **UI 계약 테스트** `tests/ui-contract.test.js`: [hidden] 가드·시작 게이트·에셋 존재·
  FX 상한·Math.random 계층 규칙을 정적으로 고정
- 보스전에 "필요 DPS vs 내 DPS" 상시 표시, 터치 타깃 44px, `user-scalable=no` 제거,
  DEV Reset 이 온보딩까지 전체 초기화

### P4 콘텐츠 (완료)

- **별빛 시련**: 보스 러시 던전, 하루 3회. 결과는 결정론 계산 + 결과 팝업 연출.
  첫 보스 = 본편 보스 35% → ×1.35 연전. 보상 = 조각 + 골드. 최고 기록 저장
- **일일 과제 3종**: 처치 200 / 강화 20 / 보스 도전 10 → 조각. 진행도 = stats − 자정 스냅숏
- **도감**: 만난 몬스터·보스 20종 기록, 미조우는 실루엣+???
- HP·생존형 던전은 POST-v1 후보로 이연(HP 축 도입 = 밸런스 전면 재작업)

### P5 연출 (완료)

- WebAudio 합성 효과음 12종 + HUD 🔊 토글(`audio.js`, 실자산 1:1 교체 가능 구조)
- 보스 돌파 셰이크+컨페티, 유물·던전 아이콘 4종 imagegen 교체
- 결과 화면은 의도적으로 없음 — 방치형은 세션이 안 끝난다. 성취 표면 = 도감·과제·던전 기록

### P3 유물 (완료, 2026-08-15)

- 별조각(보스 드랍, **실패해도 지급**) + 유물 3종(망치/곳간/나침반, 레벨당 곱연산)
- 게이트("막혔을 때 수단 둘 이상")는 `tests/balance.test.js` "유물이 벽을 깬다"가
  직접 증명 — 골드를 봉인한 막힘 상태에서 나침반만으로 벽이 뚫린다
- 24h 최장 벽 90분 → 54분. **연속 플레이 기준 하루 벽 ~50분은 구조적 하한**
  (컴퍼스 비용은 지수, 조각 수입은 선형 — 120조합 서치로 확인). 이 구간은 오프라인
  보상으로 넘기는 게 장르의 의도이고, 시뮬은 오프라인을 모델링하지 않아 비관적이다
- 재튜닝: mobHpGrowth 1.172→1.18, goldGrowth 1.15→1.158 (유물 포함 재서치 1위)

### 사용성 정리 (같은 날)

- 인트로에서 설명 문단 제거 — 로고+시작 버튼만. 조작 교육은 **코치마크 튜토리얼**이
  대신한다: 첫 강화가 가능해지는 순간 그 버튼 위에 손가락(👆)이 뜨고, 누르면 끝
- 토스트 어투 정리: "시간 초과 — 조금 더 강해져야 해요" → "아깝다! ⭐+N",
  "합류! 편성에서 넣어보세요" → "합류!" (편성 탭 점 뱃지가 유도를 대신함)

### P1 게이트 판정 (2026-08-15)

사용자가 직접 플레이할 여유가 없어 **에이전트가 판정했다.** 통과로 본 근거:

| 항목 | 측정값 |
|---|---|
| 첫 강화까지 | 14.5초 |
| 첫 보스 도달 | 30초 |
| 첫 보스 돌파 | 53.5초 |
| 스테이지 평균 체류 | 43초 (30분 구간) |
| 최장 벽 | 46초 (30분) · 2분56초 (2시간) |
| 돌파 여유비 | 1.14~1.37 (보스가 관문으로 기능) |

"막힘 → 강화 → 돌파" 리듬이 실제로 반복되고, 브라우저에서도 60초에 8스테이지 돌파 +
보스 실패 2회가 관측됐다. **다만 "한 번 더 강화하고 싶은가"는 끝내 사람의 감각이므로,
나중에 직접 플레이한 뒤 뒤집을 수 있다.** 뒤집힌다면 아래 3절의 후보부터 손본다.

### P2 편성 (완료)

- 말랑프렌즈 5명, 각자 다른 축을 민다 — 모찌(공격) · 피치(속도) · 푸딩(골드) ·
  라떼(보스 피해) · 민트(이동 시간)
- 해금은 스테이지 도달로만(가챠 없음): 1 / 5 / 15 / 30 / 50
- 파티 슬롯도 진행에 따라 열린다: 스테이지 1 / 10 / 40
- **P2 게이트 자동 검증**: 파밍 최적 조합과 돌파 최적 조합이 서로 다른지를
  `tests/balance.test.js` 가 매번 확인한다(같아지면 캐릭터가 스킨이라는 뜻).
- 편성한 1번 자리 친구가 실제 화면의 주인공 스프라이트가 된다.

| 항목 | 상태 |
|---|---|
| 순수 결정론 엔진 `engine/combat.js` | 완료 — 이벤트 기반 step, dt 크기 무관 |
| BALANCE 단일 상수 `engine/balance.js` | 완료 |
| seeded RNG `engine/rng.js` | 완료 (현재 전투는 무작위 요소 없음, 향후 드랍용) |
| 세이브 `engine/save.js` | 완료 — version 5 (v5=storySeen) + migrateSave + 상위버전 거부 |
| 챕터 데이터 `data/chapters.js` | 완료 — **7챕터**(들판→언덕→전선→심장부→정원→별빛 바다→달 공장) |
| 헤드리스 시뮬 `tools/sim.js` | 완료 — 24h 시뮬 0.2초 + **오프라인 세션 모델 `--sessions=15/120`** |
| 그리드 서치 `tools/tune.js` | 완료 — 120조합 11.6초 |
| 회귀 테스트 | **정적 67개**(balance 36 + UI 계약 13 + 연출 큐 18) + **실브라우저 124개**(first-visit 10 + multi-tab 6 + stage 108) |
| 게임 화면 `index.html/game.js/style.css` | 완료 — 횡스크롤 자동 전진, 강화 3축 x1/x10/MAX, 탭 점진 공개 |
| `?dev=1` 치트 패널 | 완료 — 골드/스테이지/오프라인/배속 x20/세이브 |
| 캐릭터 `data/characters.js` | 완료 — **6명**(수달 포함), 해금 1/5/22/55/90/120, 슬롯 1/10/40, 파티 보너스 6축 |
| 에셋 | 완료 — 총 100파일. 배경·전경 10 + **시차 far 7·sky 2**, 몹 15, 보스 8, 주인공 **걷기 6f·달리기 6f·공격 6f·스킬 8f 각 6명**(+구 단일 포즈 폴백 6), 초상 6, 적 기계 3, 아이콘 7, 이펙트·파티클·로고 |
| 실험실 노출 | 완료 — `games/registry.js` 에 `stage:'LAB'` 등록 |

### 검증한 것

```bash
node games/mallang-idle/tests/balance.test.js              # 36 passed
node games/mallang-idle/tests/ui-contract.test.js          # 13 passed
node games/mallang-idle/tests/queue.test.js                # 18 passed
node games/mallang-idle/tests/first-visit.browser.test.js  # 10 passed (헤드리스 실브라우저)
node games/mallang-idle/tests/multi-tab.browser.test.js    # 6 passed (헤드리스 실브라우저)
node games/mallang-idle/tests/stage.browser.test.js        # 108 passed (헤드리스 실브라우저)
node games/mallang-idle/tools/sim.js --minutes=5           # baseline + upper 두 시나리오 출력
node games/mallang-idle/tools/tune.js --top=5
node scripts/validate-games.js                             # registry 검증 통과
```

브라우저(로컬 정적 서버 8090, `?dev=1`)에서 확인한 것 — 콘솔 에러 없음, 적 이미지 로드,
강화 즉시 DPS 반영(10 → 11.2), 60초 진행에 8스테이지 돌파 + 보스 실패 2회(벽이 실제로 생김).

---

## 완성도 평가와 개선 지도 (17차, 2026-08-17)

> 사용자 질문 *"평타만 있는 것 같은데, 캐릭터별 특성이 있나?"* 에 대한 정직한 답과,
> 지금 시점의 층별 완성도 판단.
>
> ~~"에이전트가 만들 수 있는 것은 사실상 다 만들었다"~~ — **이 프레임은 18차에서
> 폐기했다**(사용자: *"스스로 판단해라, 할 수 있다"*). 사람 게이트가 남아 있다는
> 사실은 개선을 멈출 이유가 아니다. 에이전트는 플레이어 눈으로 스스로 판단해
> 다음 개선을 정하고 실행한다. 사용자 결정이 필요한 항목(공식 규칙 뒤집기)만 예외.

### 층별 완성도

| 층 | 판정 | 근거 / 남은 것 |
|---|---|---|
| 엔진·밸런스 | **완성 (v1 범위)** | 결정론 엔진+시뮬 공유, 벽·여유비 지표 회귀 고정. 사람 체감 재조정만 열려 있음 |
| 콘텐츠 볼륨 | **완성 (v1 범위)** | 2막 7챕터·6캐릭터·유물 3종·던전·과제·도감·스토리/구출 컷신. 챕터 8+는 데이터 추가만으로 확장 |
| 캐릭터 차별화 | **시스템·수치·그림 3층 완성** | 성장 축 6종(전원 상이) + 전용 안무 6종 + 리듬 3계층 + 스킬 FX 6종 + **17차: 캐릭터별 공격/스킬 시트**. "평타만 있다"는 인상은 단일 포즈 시절 것 — 이제 그림까지 다르다. 남은 층위는 **기능적 차별화**(액티브 스킬·영구 성장층 — 아래 개선 1) |
| 전투 연출 | **구현 완료 (개편 단계 0~5 + 에셋)** | 연출 큐·캔버스 FX·다층 시차(전용 아트)·6f 걷기/달리기·안무 시트·카메라·보스 개성·오디오 싱크·감속 접근성. 남은 것: 실기기 성능·20초 첫인상(사람) |
| 검증 | **기계 검증 포화 상태** | 정적 67 + 실브라우저 124 + 시뮬 + 시각 스모크. **재미·체감·발열은 기계가 판정 불가** |

### 개선할 부분 (우선순위순)

1. **[차단] 사람 게이트 3종** — 이것 없이는 다음 단계 착수 불가(정책):
   ① 20초 첫인상("오 귀엽고 시원하다" vs "그냥 방치형이네") ② 실기기 성능(중급기
   5분 발열·저사양 파티클 가독성 — 결정 6②) ③ P1 재미 게이트("5분 뒤 한 번 더
   강화하고 싶은가" + 5층 첫 벽 체감·해금 공백감·2막 진입 체감)
2. **[대형] 장기 영구 성장층** — codex 재미 리뷰 #3 이월. 챕터를 늘려도 **규칙이
   같아서** 깊이가 안 는다. 캐릭터 친밀도/스킬 노드/주간 보스 같은 초기화 없는 성장층이
   유력 후보. "기획의 디테일"을 늘리는 다음 층위가 바로 여기다(캐릭터별 강화 백로그와
   합류 지점). 착수 전 밸런스 전면 재측정 필요
3. **[중형] 하루 케이던스·던전 변칙** — 리뷰 #7·#9 이월: 던전 3회 연타 소모(오전/저녁
   충전 검토), 과제 고정 목표(진행도 비례 검토), 별빛 시련의 일일 변칙(공속 특화 등).
   ~~던전이 "하는 느낌"이 없다~~ → **18차에서 해소**(리플레이 의식). 변칙·케이던스는 남음
4. **[사용자 결정 필요] 벽에서의 결정권** — 자동 재도전은 v1 공식 규칙(CORE_LOOP 3절
   사용자 결정)이라 에이전트가 못 뒤집는다. 파밍 모드/돌파 프리셋 아이디어 보류 중
5. **[소형·연출 폴리시]** (전부 표현 계층, 증분 가능):
   - 걷기↔달리기 노드 폭 차이(시트별 152~290px)로 gait 전환 순간 시각 중심이 살짝
     점프 — 시트 재가공(폭 통일) 또는 중심 보정
   - 적/보스 상태별 시트 미배치(7.2절 — 지금은 단일 이미지+찌부 변형). 몹 15·보스 8
     대상이라 배치 비용이 큼 — 사람 게이트 통과 후 판단
   - idle/hurt 시트(7.1절 선택), mid 시차 겹 분리, 막타 슬로모(액터 캔버스 승격 시)
   - 컷신 speaker `hop` 클래스가 rAF 동결 환경에서 잔류 가능(실사용 영향 미미,
     헤드리스 계측만 오염 — 테스트는 프로브로 회피 완료)
6. **[배포] 재배포 + 신규 사용자 플로우 재확인** — 8차 이후 수정분(개편 전체)이
   아직 미배포라면 배포 검증이 사람 게이트보다 먼저다

## 바로 다음에 할 일

### 1. POST-v1 — 아케이드 메타게임 ★ 다음 단계 (착수 전 사람 확인 필요)

광장 전투력 표시 · 즉석 자동 대전 · 부스 게임 성적 ↔ 키우기 재화 · 글로벌 랭킹 ·
기기 간 동기화. **전제: 저장과 경제 계산 자체를 서버 권위로 옮겨야 한다**
(ROADMAP POST-v1 절). D1 스키마·익명 ID 설계가 선행되므로 플랫폼 작업이다.

### 2. codex 재미 평가 잔여 대형 건 (8차에서 이월, 설계 필요)

scratchpad 의 평가 전문은 세션과 함께 사라지므로 요지를 여기 남긴다:

- [ ] **장기 영구 성장층** — 리뷰 #3: "챕터 추가보다 캐릭터 친밀도/스킬 노드/주간
      보스 같은 초기화 없는 성장층이 먼저". 2막은 지평선을 넓혔지만 **규칙은 그대로**다.
      기존 백로그 "캐릭터별 강화"가 유력 후보 — 착수 전 밸런스 전면 재측정 필요
- [ ] **하루 케이던스 재설계** — 리뷰 #7: 던전 3회가 한 번에 연타로 소모됨(오전/저녁
      충전 검토), 과제 목표가 진행도 무관 고정(초반 너무 쉽고 후반 너무 김 —
      진행도 비례 목표 검토)
- [ ] **던전 변칙** — 리뷰 #9: 별빛 시련이 본편과 같은 문제(DPS 체크). 일일 변칙
      (공속 특화·특정 친구 보너스) → 장기적으로 HP 생존형
- [ ] **벽에서의 결정권** — 리뷰 #6: 자동 재도전이 유저 결정을 없앤다. 단,
      "자동 재도전 = v1 공식 규칙"은 CORE_LOOP 3절의 **사용자 결정**이므로
      뒤집으려면 사람 확인 필요 (파밍 모드/돌파 편성 프리셋 아이디어 포함)
- [ ] **오프라인 → 복귀 행동 확장** — 리뷰 #8 후속: "추천 MAX 강화 → 예상 돌파층"
      1탭 흐름, 제한적 별조각/도전권 지급 검토 (8차에서 "+N레벨 가능" 힌트까지 반영)

### 3. v1 잔여 백로그 (우선순위 낮음)

- [ ] **사람 플레이 검증** — P1 게이트("5분 뒤 한 번 더 강화하고 싶은가")는 에이전트가
      지표로 판정했다. 사용자가 직접 플레이해 뒤집으면 BALANCE 상수부터 재조정.
      8차 이후 확인 포인트: 5층 첫 벽의 체감(46초가 적당한가), 해금 재배치의 공백감,
      2막 진입 체감(211층은 세션 모델 기준 ~D7)
- [ ] **HP·생존형 던전** — HP 축 도입은 밸런스 전면 재작업, 광장 연동과 함께 판단
- [ ] **챕터 8+** — `data/chapters.js` 데이터 추가만으로 확장 가능 (에셋 파이프라인은
      scratchpad imagegen 스크립트 원형 참고 — 30장 병렬 생성 → 선별 → un-blend)

---

## 이 게임에서 조심할 것

- **엔진에 전투 로직을 두 벌 만들지 말 것.** `game.js` 는 렌더와 입력만 한다.
  시뮬레이터가 같은 `engine/combat.js` 를 쓰기 때문에 여기서 갈라지면 지표가 거짓말을 한다.
- **`Math.random()` 금지는 엔진·데이터·시뮬 계층에만 적용된다** (`tests/ui-contract.test.js` 가 강제).
  표현 계층(game.js 연출 지터, audio.js 노이즈)은 결정론과 무관하므로 허용. 엔진에서
  무작위가 필요해지면 `engine/rng.js` 를 주입한다.
- **지표 회귀 테스트의 허용 범위를 조용히 넓히지 말 것.** 먼저 BALANCE 상수를 고친다.
  범위를 바꿔야 한다면 근거를 테스트 주석과 커밋 메시지에 남긴다(이미 두 건의 선례가 있다).
- **`window.__mallangIdle`** (`?dev=1` 전용) — 헤드리스/백그라운드 탭에서는
  `requestAnimationFrame` 이 멈춰 게임이 진행되지 않는다. 이 훅의 `advance(초)` 로
  시간을 직접 밀어서 렌더 결과까지 검증할 수 있다.

---

## 관련 문서

- [CORE_LOOP.md](CORE_LOOP.md) — 게임 정의와 루프
- [BALANCE.md](BALANCE.md) — 공식·상수·지표
- [ROADMAP.md](ROADMAP.md) — 단계와 게이트
- [AGENT_PROTOCOL.md](AGENT_PROTOCOL.md) — 자동 개발 작업 규약
