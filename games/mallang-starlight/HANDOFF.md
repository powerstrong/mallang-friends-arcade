# HANDOFF — 말랑프렌즈 별빛작전 (별빛 구조 퍼즐)

> 콜드 스타트 순서: 이 문서 → `REDESIGN.md`(정식 기획) → `index.html`  
> 폴더·레지스트리 ID·코드 네임스페이스는 `mallang-starlight`로 통일한다.  
> 최종 갱신: 2026-08-09

> ⚠️ **2026-08-09 전면 재설계.** 코덱스+Claude 재미 리뷰(독립적으로 동일 결론) 결과, 2인 결정론 SRPG의 재미 천장이 낮아 **「결정론 전술 퍼즐」로 장르를 교체**했다. 정식 기획은 `REDESIGN.md`. 구 SRPG 문서(`DESIGN.md`)와 구 코드(`rules.js`/`missions.js`)는 폐기·삭제됨.

---

## 0. 30초 요약

- 모바일 웹 싱글플레이 **결정론 전술 퍼즐**이다. 라떼는 옆 장난감을 **밀고**, 민트는 일직선 장난감을 **당긴다**. 장난감(포탑)을 **벽·수리별·다른 장난감**에 부딪히게 재배치해 모두 수리하면 클리어.
- 파일: `engine/puzzle.js`(순수 결정론 엔진), `data/puzzles.js`(5퍼즐), `engine/ui.js`(홈/퍼즐/결과 + 예약→미리보기→출동→되돌리기 루프).
- 코어 루프: 두 친구에게 (이동+밀기/당기기)를 **예약** → 비트 전체 결과가 **미리보기** → 큰 **출동!** 한 번으로 (친구 이동 → 능력 → 장난감 예고 실행 → 충돌/수리) 동시 해결. **되돌리기(undo)**·처음부터 지원.
- 포탑은 스스로 못 움직이고 수리되지 않음 → **밀기/당기기가 항상 필수**(재배선 메커니즘 강제). 예고: 포탑 광선 칸이 주황으로 표시(삐걱봇 돌진 화살표는 후속).
- 비폭력 톤 유지("수리"), 하트 2개, localStorage 진행 저장. 별: 클리어=1 / 무피해=2 / 최적 비트(par)=3.
- **BFS 솔버 회귀 테스트**: 각 퍼즐이 (풀이가능·무피해경로·par=최적·**능력없이는 불가=비자명**·무겹침)임을 `tests/puzzles.test.js`가 검증. Node 테스트 `puzzles` 7개 + `ui-static` 7개 통과.
- 375×812 모바일 브라우저 검증: 탭 조작(친구→칸→장난감)·예약·미리보기(`예상 · 장난감 1 수리 · 피해 0`)·출동→결과 ★★★·되돌리기·별 저장까지 확인. 콘솔 에러 0.

---

## 1. 현재 확정 결정

1. 말랑프렌즈 5종을 사용한다: 말랑 병아리, 모찌 토끼, 푸딩 햄스터, 라떼 강아지, 민트 고양이.
2. 첫 출시 로스터는 라떼 강아지(보호형)와 민트 고양이(원거리형)다.
3. 적은 고블린·오크가 아니라 삐걱봇·콩콩봇·반짝포탑·덩치곰 로봇 같은 오작동 장난감이다.
4. 밝은 구조 모험 톤. `사망/처치` 대신 `기운 소진/작동 정지/수리`를 쓴다.
5. 페이즈턴 유지, SPD 폐기.
6. 기본 피해는 고정값. 기존 ±15% 랜덤은 제거한다. RNG는 주입형 시드로만 제공한다.
7. 고정 성장, 퍼머데스 없음, 전투 후 무료 회복.
8. 목표는 전멸형만 쓰지 않고 길 확보·운반/탈출·장치 해제를 혼합한다.
9. 싱글플레이, localStorage, 메뉴 허브, 모바일 세로 우선, 무빌드 바닐라 JS를 유지한다.
10. `world/assets`와 `games/machine-animal-runner/assets`의 기존 말랑프렌즈 자산을 우선 재사용한다.

이전 HANDOFF의 “말랑프렌즈와 무관한 독립 판타지” 결정은 사용자 요청으로 명시적으로 무효화되었다.

---

## 2. 전술 명제와 첫 3개 작전

핵심 반복은 `적 미리보기 → 위치 선택 → 친구 능력 연계 → 결과 확인`이다.

1. **공방 골목을 열어라**: 라떼로 길을 막고 민트가 뒤에서 지원한다.
2. **별빛 배터리를 옮겨라**: 6턴 안에 운반 목표를 탈출 칸으로 옮긴다.
3. **시계탑을 진정시켜라**: 양쪽 제어별 2개를 켠 뒤 덩치곰 로봇을 멈춘다.

플레이테스터가 이 세 작전의 서로 다른 트레이드오프를 설명하지 못하면 성장·장비·대량 아트로 넘어가지 않는다.

---

## 3. 현재 구현 현황

현재 작업트리에는 다음이 구현되어 있다.

- 8×8 격자, 라떼/민트 2인 고정 로스터
- 라떼 `든든막기`(인접 보호)와 민트 `별빛콩`(거리 3 무반격) 고유 특기
- 순수 상태/규칙 계층 + 직렬화 가능 상태 + 주입형 시드 RNG
- LoS 포함 공격 판정, 고정 피해, 맞대응, 적 AI
- 단일 합법수 생성기 기반 이동/공격/위협/상호작용
- `forecastEnemyPhase` 결정적 적 페이즈 미리보기(비파괴): 각 장난감의 의도·예상 피해·이동 예정 칸을 미리 계산해 UI가 `적 미리보기` 버튼으로 노출. 실제 결과와 정확히 일치
- UI 문구는 표준 게임 용어로 통일: `체력/공격/방어/이동/사거리`·`피해`·`반격`·`스킬`·`쓰러짐`·`내 차례`·`적 미리보기`. 순화·창작 용어(`기운/해결/버팀`·`예고`·`친구 차례`·`작동 정지`)는 폐기. 캐릭터 명칭(친구/장난감)만 테마 유지. 자세한 결정은 DESIGN §5.3
- 온보딩: 첫 전투에서 `조작 방법` 가이드 1회 자동(`save.seenGuide`), 상시 칸 색 범례, 단계형 힌트. 재열람은 브리핑 `설명 보기`·전투 `목표 설명`
- 작전 3종
  - `alley-tutorial`: 전멸형 튜토리얼
  - `battery-run`: 배터리 픽업 → 운반 → 탈출
  - `clocktower-core`: 제어별 2개 활성화 후 보스 정지
- 홈 화면 → 브리핑 → 전투 → 결과 화면 루프
- localStorage 자동 저장과 이어하기
- 적 페이즈 중간 저장 금지와 구버전 적 페이즈 세이브 자동 재개
- PWA용 `manifest.webmanifest`
- 런타임 최적화 에셋(`assets/runtime/**/*.webp`, 앱 아이콘)
- WebAudio 절차적 사운드: 화면별 BGM 3테마 + 이벤트 효과음. 바이너리 오디오 없음, `save.sound` 토글로 일괄 제어

Phase 0 메커닉 프로토타입은 더 이상 “현재 결과물”이 아니라, 위 버티컬 슬라이스 알파의 기반 단계로 간주한다.

---

## 4. 반드시 고칠 코드 결함

- **MOBILE-ZOOM — 수정됨**: 줌 차단 제거
- **TOUCH-SIZE — 수정됨**: 44px 셀 + 좁은 화면 보드 스크롤
- **RNG-DOUBLE — 수정됨**: 기본 피해 랜덤 제거
- **DOM-IN-STATE — 수정됨**: DOM 참조를 UI 전용 맵으로 분리
- **LOS-MISSING — 수정됨**: 원거리 공격 LoS 구현
- **AUTO-WAIT — 수정됨**: 이동 후 수동 확정/취소 + 대기 선택 유지
- **JSON-OVERCLAIM — 수정됨**: 문서와 아키텍처 설명을 현실화

---

## 5. Step 1~2 구현 결과

1. [x] `engine/rules.js`에 직렬화 가능한 전투 상태 정의
2. [x] `listLegalActions(state, actorId)`를 입력·AI·미리보기·위협표시·테스트의 단일 규칙 원천으로 사용
3. [x] 고정 피해와 주입형 시드 RNG 분리, 규칙 내부 `Math.random()` 금지, 확정 행동만 RNG 스트림 전진
4. [x] 비변이 `forecastAction`과 상태 전이 `applyAction` 분리
5. [x] 이동 미리보기→확정/취소 상태기계 연결
6. [x] 적 위협범위, 전투 예측, 목표 HUD 연결
7. [x] LoS, 줌, 44px 타깃과 §4 버그 회귀 고정
8. [x] Node 순수 규칙 테스트와 정적 UI 검증
9. [x] 모바일 브라우저 수동 플레이(390×844 기준 1번 작전 진입/이동/자동저장/이어하기/특기 확정)
10. [x] `data/missions.js`에 3작전 콘텐츠 계층 분리
11. [x] 운반/탈출, 제어별 활성화, 턴 제한, 보스 목표 규칙 연결
12. [x] 홈/브리핑/전투/결과 UI와 localStorage 세이브 연결
13. [x] 3번 작전 밸런스 재조정과 전원 생존 승리 회귀 테스트

브라우저에서 파일 직접 열기를 유지하기 위해 Step 1은 비모듈 `<script src>`를 사용한다. JSON `fetch()` 외부화는 버티컬 슬라이스 이후다.

---

## 6. 테스트 불변조건

- 상태는 JSON 왕복 후 동일한 합법수를 만든다.
- 동일 상태·행동열·시드는 동일 결과를 만든다.
- 미리보기는 상태와 RNG를 소비하지 않는다.
- 합법 행동 확정은 RNG 스트림을 정확히 1회 전진시키며, 취소·불법 행동은 전진시키지 않는다.
- AI가 선택한 행동은 반드시 단일 합법수 목록에 있다.
- 벽 너머 원거리 행동과 맞대응은 불가능하다.
- 이동 취소는 위치·행동권을 바꾸지 않는다.
- 위협범위는 적의 실제 합법 공격 도달 칸과 일치한다.
- 목표 완료 여부는 UI 문구가 아니라 순수 상태에서 계산한다.

---

## 7. 로드맵

- **Phase 0**: 메커닉 프로토타입(완료, 기반 단계)
- **Step 1**: 전술 가독성 + 순수 계층 + 버그 수정(완료)
- **Step 2**: 서로 다른 작전 3개(완료)
- **Step 3**: 하드 재미 게이트(자동 승리 경로 검증 완료, 실기기 감각 조정만 남음)
- **Step 4**: 1챕터 버티컬 슬라이스(구현 완료, DRAFT registry 등록 완료)
- **Step 5**: 캐릭터·캠페인·최종 아트 확장

---

## 8. 아트 상태

- 기존 말랑프렌즈 기준: `world/assets/*portrait.png`, `*sheet_3x3.png`
- 기계 적 기준: `games/machine-animal-runner/assets/enemy-mech-*.png`, `boss-mech.png`
- 상세 자산 계약: `ART.md`
- 폐기된 중세 시안, 미사용 타일, 중복 PNG 원본, 미등장 친구 토큰은 저장소에서 제거했다. 필요하면 Git 이력에서 복구한다.
- 신규 이미지는 `assets/ASSET_BRIEF.md` 계약에 따라 별도 작업 공간에서 만들고, 채택된 최적화 런타임 에셋과 앱 아이콘만 저장소에 넣는다.

---

## 9. 작업 규약

- 사용자 응답은 한국어 존댓말.
- 별도 브랜치 없이 `main`에서 작업한다.
- 폴링/대기 루프 금지.
- `games/registry.js`에 `DRAFT` / `DIRECT_ONLY` / `NOT_SUBMITTED` 상태로 등록한다. 광장 공개 승격 전까지는 직접 URL 위주로 확인한다.
- 기존의 다른 게임/사용자 변경을 건드리거나 되돌리지 않는다.
- 문서와 실제 구현이 달라지면 같은 작업에서 HANDOFF/DESIGN을 갱신한다.

---

## 10. 다음 작업자가 읽을 파일

```text
games/mallang-starlight/
  HANDOFF.md
  DESIGN.md
  ART.md
  index.html
  manifest.webmanifest
  engine/rules.js
  engine/ui.js
  data/missions.js
  tests/rules.test.js
  tests/missions.test.js
  tests/ui-static.test.js
  assets/ASSET_BRIEF.md
  assets/runtime/                # 코드가 실제 사용하는 배포 자산만 보관
```

현재 가장 중요한 다음 작업은 **실기기 2·3번 작전 재확인, 결과 연출/사운드 세부 폴리시, 공개 승격 전 최종 QA**다. 별 보상은 구현됐고 병아리 합류는 후속 챕터로 결정했다. 구조적 경계는 `콘텐츠 → 명령 → 순수 규칙/상태 → 이벤트 → UI`로 유지한다.
